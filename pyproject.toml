[project]
name = "quakewatch"
version = "1.0.0"
description = "Flask dashboard for USGS earthquake data"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "matplotlib>=3.10.9",
    "requests>=2.34.0",
]
